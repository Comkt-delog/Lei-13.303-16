[build-system]

requires = ["flit_core >=1.0"]

build-backend = "flit_core.buildapi"
 
[project]

name = "Manual Lei 13.303/16 (COMKT)"

authors = [{name = "Teste", email = "comkt.delog@gestao.gov.br"}]

dynamic = ["version", "description"]
